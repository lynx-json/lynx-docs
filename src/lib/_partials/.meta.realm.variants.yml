header: "Variants ({{{variants.length}}})"
variants>list:
  value@variants:
    - href<url:
      title<name:
      type<: application/lynx+json
      template: "Template: {{{template}}}"
      data: "Data: {{{data}}}"
