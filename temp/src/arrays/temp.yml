# Lynx Template
items@:
  name<:

# Lynx Template (Expanded)
spec:
  hints: []
value:
  items@:
    spec:
      name: items
      hints: []
    value:
      spec:
          hints: []
      value:
        name<:
          spec:
            hints: []
            name: name
          value: null

# Lynx Template (Export to Handlebars)
spec:
  hints: [ container ]
  children:
    - name: items #1
value:
  items:
    spec: #1
      name: items
      hints: [ container ]
    value: [
      {{#each items}}
      {
        spec:
          hints: [ container ]
          children:
            - name: name
        value:
          name:
            spec:
              hints: [ text ]
            value: {{#if name}}{{name}}{{/if}}
      }{{#unless @last}},{{/unless}}
      {{/each}}
    ]

# Lynx Document (Expanded)
spec:
  hints: [ container ]
  children:
    - name: items #1
value:
  items:
    spec: #1
      name: items
      hints: [ container ]
    value:
      - spec:
          hints: [ container ]
          children:
            - name: name #2
        value:
          name:
            spec: #2
              hints: [ text ]
            value: Dan
      - spec:
          hints: [ container ]
          children:
            - name: name #2
        value:
          name:
            spec: #2
              hints: [ text ]
            value: John

# Lynx Document (Collapsed)
spec: /specs/1.lnxs
items:
  - name: Dan
  - name: John
