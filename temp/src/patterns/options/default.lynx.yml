>page:
  globalNav>:
  main:
    header: Options
    textOptions:
      spec:
        hints: [ card, section ]
      value:
        header: Text Options
        color>input:
          label: Colors
          hints: [ color, line ]
          options@colors:
            - spec:
                hints: [ color ]
                option: true
              value<color:
    cardOptions:
      spec:
        hints: [ card, section ]
      value:
        header: Card Options
        color>input:
          label: Colors
          hints: [ color, line ]
          options@colors:
            - spec:
                hints: [ card, section ]
                option: true
              value:
                header<title:
                description: Lorem ipsum dolor sit amet, consectetur adipisicing elit.
                code:
                  spec:
                    hints: [ color ]
                    visibility: hidden
                  value<color:
